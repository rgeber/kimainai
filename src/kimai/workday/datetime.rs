use std::sync::Arc;
use crate::args::FileWorkdayArgs;
use crate::error::AppError;
use crate::state::AppState;

pub fn get_workday_start_time(app_state: Arc<AppState>, args: &FileWorkdayArgs) -> Result<(), AppError> {



    Ok(())
}